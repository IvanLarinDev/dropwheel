<UserControl x:Class="Dropwheel.UI.ToastHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Visibility="Collapsed" HorizontalAlignment="Left">
    <Border x:Name="Root" CornerRadius="8" Padding="10,6" MaxWidth="300"
            MouseEnter="OnMouseEnter" MouseLeave="OnMouseLeave">
        <StackPanel Orientation="Horizontal">
            <Ellipse x:Name="KindDot" Width="7" Height="7" VerticalAlignment="Center" Margin="0,0,7,0"/>
            <TextBlock x:Name="MessageText" Foreground="White" FontSize="12" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis" MaxWidth="220"/>
            <TextBlock x:Name="UndoLink" Text="  Undo" FontSize="12" FontWeight="SemiBold"
                       VerticalAlignment="Center" Cursor="Hand" Visibility="Collapsed"
                       MouseLeftButtonUp="OnUndo"/>
        </StackPanel>
    </Border>
</UserControl>
