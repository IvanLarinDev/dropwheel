<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="clr-namespace:Dropwheel.UI">

    <Style TargetType="{x:Type ui:DialogShell}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:DialogShell}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel x:Name="HeaderHost" Grid.Row="0" Margin="18,16,18,10">
                            <TextBlock Text="{TemplateBinding Title}" FontSize="15"
                                       FontWeight="SemiBold" TextWrapping="Wrap"/>
                            <TextBlock x:Name="SubText" Text="{TemplateBinding Subtitle}" FontSize="12"
                                       Margin="0,2,0,0" TextWrapping="Wrap"
                                       Foreground="{DynamicResource DwTextMuted}"/>
                        </StackPanel>

                        <ContentPresenter Grid.Row="1" Margin="18,0,18,0"/>

                        <Border Grid.Row="2" Margin="0,14,0,0" BorderThickness="0,1,0,0"
                                BorderBrush="{DynamicResource DwBorder}">
                            <DockPanel Margin="18,12,18,14">
                                <Button x:Name="PART_Danger" DockPanel.Dock="Left"
                                        Content="{TemplateBinding DangerText}"
                                        Foreground="{DynamicResource DwDanger}"
                                        Padding="8,4" HorizontalAlignment="Left"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="PART_Cancel" Content="Cancel" Width="90"
                                            Padding="8,4" Margin="0,0,8,0" IsCancel="True"/>
                                    <Button x:Name="PART_Primary" Content="{TemplateBinding PrimaryText}"
                                            MinWidth="96" Padding="12,4" IsDefault="True"
                                            IsEnabled="{TemplateBinding IsPrimaryEnabled}"/>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Title" Value="{x:Null}">
                            <Setter TargetName="HeaderHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Title" Value="">
                            <Setter TargetName="HeaderHost" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Subtitle" Value="{x:Null}">
                            <Setter TargetName="SubText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="Subtitle" Value="">
                            <Setter TargetName="SubText" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="DangerText" Value="{x:Null}">
                            <Setter TargetName="PART_Danger" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="DangerText" Value="">
                            <Setter TargetName="PART_Danger" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="ShowCancel" Value="False">
                            <Setter TargetName="PART_Cancel" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
