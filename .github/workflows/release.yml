name: Release

# Tag push like v1.2.3 -> publish single-file exe -> zip -> GitHub Release
on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # actions/checkout@v7

      - uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Publish (framework-dependent, single file)
        run: dotnet publish src/Dropwheel -c Release -o dist-fd

      - name: Publish (self-contained, single file)
        run: >
          dotnet publish src/Dropwheel -c Release -r win-x64 --self-contained true
          -p:IncludeNativeLibrariesForSelfExtract=true -o dist-sc

      - name: Zip
        shell: pwsh
        run: |
          Compress-Archive -Path dist-fd/* -DestinationPath "Dropwheel-${{ github.ref_name }}-win-x64.zip"
          Compress-Archive -Path dist-sc/* -DestinationPath "Dropwheel-${{ github.ref_name }}-win-x64-self-contained.zip"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # softprops/action-gh-release@v3
        with:
          files: Dropwheel-*.zip
          generate_release_notes: true
          body: |
            Dropwheel — overlay launcher & drag-and-drop wheel for Windows 10/11.
            Unzip and run Dropwheel.exe. Config: %AppData%\Dropwheel\config.json

            - `win-x64.zip` — small, requires .NET 10 Desktop Runtime
            - `win-x64-self-contained.zip` — larger, no runtime needed
